const slideContainer=document.querySelector(".container"),slide=document.querySelector(".slides"),nextBtn=document.getElementById("next-btn"),prevBtn=document.getElementById("prev-btn"),interval=3e3;let slideId,slides=document.querySelectorAll(".slide"),index=1;const firstClone=slides[0].cloneNode(!0),lastClone=slides[slides.length-1].cloneNode(!0);firstClone.id="first-clone",lastClone.id="last-clone",slide.append(firstClone),slide.prepend(lastClone);const slideWidth=slides[index].clientWidth;slide.style.transform=`translateX(${-slideWidth*index}px)`,console.log(slides);const startSlide=()=>{slideId=setInterval(()=>{moveToNextSlide()},3e3)},getSlides=()=>document.querySelectorAll(".slide");slide.addEventListener("transitionend",()=>{(slides=getSlides())[index].id===firstClone.id&&(slide.style.transition="none",index=1,slide.style.transform=`translateX(${-slideWidth*index}px)`),slides[index].id===lastClone.id&&(slide.style.transition="none",index=slides.length-2,slide.style.transform=`translateX(${-slideWidth*index}px)`)});const moveToNextSlide=()=>{slides=getSlides(),index>=slides.length-1||(index++,slide.style.transition=".9s ease-out",slide.style.transform=`translateX(${-slideWidth*index}px)`)},moveToPreviousSlide=()=>{index<=0||(index--,slide.style.transition=".9s ease-out",slide.style.transform=`translateX(${-slideWidth*index}px)`)};slideContainer.addEventListener("mouseenter",()=>{clearInterval(slideId)}),slideContainer.addEventListener("mouseleave",startSlide),nextBtn.addEventListener("click",moveToNextSlide),prevBtn.addEventListener("click",moveToPreviousSlide),startSlide();